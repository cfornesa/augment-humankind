<!-- Agent reads this file at every session start. Surface any entry marked PENDING CONFIRMATION
to the human before proceeding. Do not act on a pending entry — wait for explicit confirmation
or rejection. -->

2026-06-11 STACK Augment Humankind v1 is a no-framework PHP site with durable routes at `/`, `/services`, `/notes`, and `/contact`.

2026-06-11 DECISION The v1 brand direction is Fieldguide for nontechnical teams, with the friendly robot used as a primary mascot signal.

2026-06-12 DECISION The `/contact` page uses a reCAPTCHA v3-protected PHP form with PHPMailer over Hostinger SMTP and no database or submission storage.

2026-06-12 DECISION `/services` and `/notes` are now DB-backed via the Pages CMS (with static fallback); new pages are created in `/admin/pages` and reach the public site via a catch-all `/{slug}` route.

2026-06-12 DECISION Page section editing in `/admin` uses the Tiptap rich-text editor loaded from the esm.sh CDN; it falls back to a plain textarea if the CDN is unreachable. The media picker is now wired to flat `/admin/media/*` endpoints for library, upload, and import.

2026-06-12 NOTE The contact-config verification harness (`scripts/verify-contact-config.php`) was switched from probing `/notes` to `/contact`, because DB-backed managed pages now `exit` early — relevant if routing changes again.

2026-06-12 DECISION Phase 3/4 admin CMS uses flat protected routes: `/admin/artworks`, `/admin/categories`, `/admin/exhibits`, `/admin/media`, `/admin/trash`, and `/admin/navigation`. Public navigation is backed by `navigation_items` with fallback to Mission, Services, Field Notes, Contact, and Portfolio.

2026-06-14 DECISION Platform assimilation uses the current PHP MySQL database as the only writable target; the live platform database is read/export-only via `PLATFORM_*`; canonical feed route is `/blog`.

2026-06-14 DECISION Phase 3 (auth/admin/CMS reconciliation) is complete. The nav_links migration mapping bug (platform `kind='system'` rows silently deleted by `removeDefunctSystemItems()`) is fixed; the dropped "Feeds" and "Categories" links are repaired as visible externals at `/blog/feeds` and `/blog/categories`. The public header now has 11 items: Home, Services, Field Notes, Blog, Contact, Portfolio, About, Exhibit, Coded Art, Feeds, Categories.

2026-06-14 DECISION Phase 4A (Admin Content Core) is complete, no schema changes: `/admin/posts` (CRUD for draft/published/scheduled blog posts, with categories and featured image) and `/admin/comments` (comment/reaction moderation, `?tab=comments|reactions`) are now live, with recycle-bin coverage for posts and comments at `/admin/trash`. Phase 4 is now sequenced 4B-4H in `docs/platform-assimilation-plan.md`.

2026-06-14 DECISION Phase 4B (Scheduled publishing + feed export parity) is complete. `publishDuePosts()` flips `scheduled` → `published` and overwrites `created_at`. New routes: `/feeds/mf2`, `/blog/category/{slug}/feed.*`, `/{slug}/feed.*`, plus legacy 301 redirects. Existing 6 feed routes enhanced with `subtitle`, `author`, `summary`, `category`, `description`, `authors`, `content_text`, `tags`. `/export.json` remains JSON Feed 1.1 per Rule 5; `/feeds/mf2` is the mf2 equivalent.

2026-06-14 DECISION Phase 4C (Art pieces & platform exhibits) is complete. Platform art pieces live at `/pieces/*` with admin at `/admin/pieces/*`. New models: `PlatformArtPiece`, `PlatformArtPieceVersion`, `PlatformExhibit`. Public render uses iframe for p5.js or CSS output. Existing portfolio at `/portfolio/*` preserved per Rule 5.

2026-06-14 DECISION Phase 4D (Feed ingestion & moderation) is complete. `FeedSource` model with CRUD, `feed-ingest.php` helper for RSS/Atom parsing, `FeedSourcesAdminController` with sources table and pending-import moderation queue. Admin nav now includes "Feeds".

2026-06-14 DECISION Phase 4E (Site identity & media) is complete. `SiteAsset` and `MediaAsset` models, `SiteIdentityAdminController` with three-tab admin (Settings, Assets, Media Library). Admin nav now includes "Identity".

2026-06-14 DECISION Phase 4F (User profiles & AI settings) is complete. `UserAiVendorSettings` and `UserAiVendorKeys` models, AES-256-GCM encryption helper (`encryption.php`), `UserProfilesAdminController` with three-tab admin (Users, AI Settings, API Keys). Admin nav now includes "Users".

2026-06-14 DECISION Phase 4G (Platform connections & syndication foundation) is complete. `PlatformConnection` and `PostSyndication` models, `PlatformConnectionsAdminController` with two-tab admin (Connections, Syndications). Scaffold only — no live adapters. Admin nav now includes "Connections".

2026-06-14 DECISION Phase 4H (Syndication adapters) is complete. All 8 platform adapters implemented: Bluesky, WordPress.com, WordPress self-hosted, Blogger, Substack, LinkedIn, Facebook, Instagram. Uses `guzzlehttp/guzzle` as shared HTTP client. `AdapterFactory` maps platforms to adapter instances. `SyndicationPayload` normalizes post data. Content helpers ported from platform's `content.ts`. `PlatformConnectionsAdminController::publish()` orchestrates syndication.

2026-06-14 DECISION Platform Rectification Pass Round 2 is complete: 9 more admin views fixed (Closure-vs-`ob_start()` bug), `/admin/posts`'s second deprecation (`null =>` array key) fixed, shared admin CSS vocabulary (`.admin-container`, `.admin-tabs`, `.admin-link`, `.status-badge`, `.field`, `.form-status`, etc.) added to `admin.css`, piece-prompt overflow fixed, Immersive/VR links surfaced, `/admin/pieces/{id}/edit` fixed with a live preview pane, and `/admin/media` now unifies `media_files` + the 102 migrated `media_assets` (read-only).

2026-06-14 NOTE The platform's "VR mode" is the Three.js immersive presentation at `/immersive/pieces/{id}` (`ImmersiveController::piece`) — not WebXR/A-Frame, which was already rolled back. Now linked from `/pieces/{id}` and `/admin/pieces`.

2026-06-14 NOTE Recurring bug pattern in admin views: `$content = function () use (...): void { ... };` produces a Closure that `<?= $content ?>` in `layout.php` can't stringify (fatal). Correct pattern is `ob_start(); ... $content = ob_get_clean();`. Watch for this in any new/copied admin view.

2026-06-15 DECISION Fixed the art piece engine validation whitelist to support `p5`, `c2`, `three`, and `svg` (dropped invalid `css`). Restructured `/admin/pieces/{id}/edit` and `/admin/pieces/create` into Metadata/HTML/CSS/JS tabs that edit the `current_version` in place.

2026-06-15 DECISION Wired migrated `media_assets` CRUD parity under `/admin/media` and `/admin/trash` via `/admin/media/asset/{id}/update|trash|destroy` routes, supporting metadata updates, trashing, restoring, and permanent purging.

2026-06-15 DECISION Implement AI Piece Generation (Round 4): created Guzzle-based multi-vendor client (AiProviderClient.php), GET/POST generation routes under /admin/pieces/generate, and preview/save templates. Uses a 3-attempt validation and repair loop checking static constraints and window.sketch definitions.

2026-06-15 DECISION Implement Immersive/VR Gallery Overhaul (Round 5): completed full-immersion Three.js pieces (/immersive/pieces/{id} where engine === 'three'), gallery room framing for P5.js, C2.js, SVG, and image pieces, and the multi-frame progressive exhibit wall (/immersive/exhibits/{slug}). Built dynamic client-side iframe lazy-loading (public/embed.js) that intercepts and upgrades embeds inside blog posts and CMS pages using IntersectionObserver. Fully verified that check-platform-deletion-readiness.php passes with HTTP check validation.

2026-06-15 DECISION Platform-assimilation gap closure (items 1-7) is complete: `scripts/repair-platform-embed-links.php` normalizes embed iframe URLs; new public `/exhibits` + `/exhibits/{slug}`; `/admin/platform-exhibits` (+`/library`) read-only admin listing distinct from native `/admin/exhibits`; TipTap "Insert art piece or exhibit" picker backed by `/admin/pieces/library`; iframe-embed and AI-profile `<dialog>` pickers replace both `window.prompt()` calls; `readiness_check_post_embeds()` added to `check-platform-deletion-readiness.php`; VR/immersive pages (piece/image/exhibit) now show full metadata parity with the legacy platform (About this piece, fixed image description, Artist Statement/Biography/Works cards).

2026-06-15 NOTE The `<dialog>`-based `.media-picker-*` pattern (header/tabs/panel/grid/item/footer, plus `.media-picker-field-label/-textarea/-select`) is now the standard for all TipTap insertion UIs (media, pieces/exhibits, iframe, AI profile) — reuse this for any future insertion dialog instead of `window.prompt()`.

2026-06-15 DECISION Resolved abstract-studies exhibit embed: Added iframe_code column to platform_exhibits schema. Inserted u276695328_augmentart record for abstract-studies (ID 6) containing the external iframe source. Normalized Post #9 (posts.id=9) URL to relative /immersive/exhibits/abstract-studies?embed=1. Exhibits Controller and embed.js updated to render iframe_code directly. All link repair and readiness bypass exceptions removed. Platform deletion is now fully unblocked.

2026-06-15 DECISION Renamed native portfolio elements ('Works' -> 'Exhibit', 'Exhibits' -> 'Collections') and platform exhibits ('Platform Exhibits' -> 'Platform Collections') in the target PHP database and routes, using the Pure approach. Replaced database text field route references (in posts, page sections) in accordance with the new scheme. Modified models (Collection, Exhibit, ExhibitMediaItem, PlatformCollection), controllers, trash flow, and templates to align with the new scheme. Modified media library views and TipTap editor selection grids to support iframe/embed kinds with HTML copy templates and preview rendering. Verified all check-platform-deletion-readiness.php verifications pass (100% PASS with HTTP checks).

2026-06-15 DECISION Implemented the dual-mode VR/immersive interface on PHP views (piece.php, collection.php, image.php) using the CSS-driven Overlay approach, supporting standard split layout and fullscreen canvas-only immersive layout.
2026-06-15 DECISION Implemented iframe embed code copy actions (plain, custom interactive, and CMS interactive) on immersive views via navigator.clipboard and custom toast feedback.
2026-06-15 DECISION Fixed Three.js and C2.js runtime initializers in piece-render.php by exposing window.THREE globally and loading the c2.js library on-demand.

2026-06-15 DECISION Integrated split-pane editor (desktop), full-canvas (tablet/mobile), and revertible AI prompt-centric refinement (Reframe) into a unified workspace under `/admin/pieces/create` and `/admin/pieces/{id}/edit`.

2026-06-15 DECISION Three.js runtime is now consistent across all four rendering surfaces: `public/app/helpers/piece-render.php`, `public/app/views/admin/pieces/form.php`, `public/app/views/admin/pieces/generate-preview.php`, and `public/embed.js`. Each uses `instrumentedThree`, `autoFit`, `ensureFallbackLighting`, `OrbitControls`, `startFrame(count)`, and `WebGLRenderer({ canvas })`. Any future Three.js fix must be mirrored in all four files.

2026-06-15 DECISION AI Refine endpoint (`POST /admin/pieces/refine-ai`) hardened with SVG edge-case handling (default `window.sketch = () => {};` when JS block is omitted) and default HTML/CSS fallbacks when the AI omits blocks. The same 3-attempt retry/repair loop and preflight validation as generation are used.

2026-06-15 NOTE Two test suites created in `tests/`: `art-piece-generation.php` (25 tests for code extraction, validation, prompts) and `three-runtime-consistency.php` (36 tests for runtime parity across all rendering files). Run with `php tests/art-piece-generation.php` and `php tests/three-runtime-consistency.php`.

2026-06-15 DECISION Resolved browser timeouts/freezes in immersive/default VR modes by correcting DOM traversal sanitization to remove traversed child nodes. Restored Three.js and Full Canvas rendering by binding the global THREE instance and overriding WebGLRenderer render/setSize methods to capture scene and camera references across all four rendering paths.

2026-06-15 DECISION Fixed collection/image embed rendering in blog posts by changing template.appendChild to template.content.appendChild inside public/embed.js web components. Corrected Tiptap insert picker dialog and script in layout.php and tiptap-editor.js to reference Collections instead of Exhibits, using the correct grid selector and load state variables.

2026-06-15 DECISION Hardened Three.js camera auto-fit bounding box calculations to ignore skyboxes (BackSide materials), particles (Points), large objects (dimensions >= 30), and large planes (dimensions >= 15). If no valid artwork bounding box is found, the camera defaults to the sketch's pre-configured position instead of zooming to infinity.

2026-06-15 DECISION Hardened Three.js camera auto-fitting to preserve custom camera positions (lengthSq > 0.01) and zoomed in default auto-fit views 3.5x.
2026-06-15 DECISION Appended returnTo query parameters to all VR gallery buttons in embed.js and public views, enabling the Back button to route to referring blog posts.
2026-06-15 DECISION Enabled route dispatching for /collections paths in public/index.php gate, restoring platform collection detail rendering.
2026-06-15 DECISION Implemented batched 3-item progressive see-more gallery disclosure on portfolio.php.
2026-06-15 DECISION Portfolio taxonomy is now split three ways: `/admin/categories` manages blog post categories, `/admin/art-media` manages piece taxonomy (`category_scope='portfolio'`), and `/admin/exhibit-collections` is the renamed native collection surface. Public redirects keep `/portfolio/collections`, `/portfolio/categories`, and `/portfolio/category/{slug}` durable while canonical routes are `/portfolio/exhibit-collections` and `/portfolio/art-media`.
2026-06-15 DECISION Aligned blog action buttons via flexbox, keeping Edit/Expand at the top-right and Embed/Share/Comments at the bottom.

2026-06-16 DECISION Fixed mobile viewport overflows by restricting embed elements and web components to max-width: 100% and reducing min-height on small viewports.

2026-06-16 DECISION Upgraded the art piece thumbnail capture flow with allow-same-origin sandboxing, dirty-check auto-capture-on-save, and a sequential bulk-regeneration utility in the admin index.

2026-06-16 DECISION Added close-up Three.js gallery room thumbnail generation for Platform Collections, including a database-backed schema option, conditional WebGL preserveDrawingBuffer properties, and auto-capture-on-save form interception.
2026-06-16 DECISION Resolved blank SVG thumbnails by copying computed DOM styles inline to vector nodes before canvas serialization, disabling transient CSS animations during capture.
2026-06-16 DECISION Synced platform collection edit form states with hidden input thumbnail fields and AJAX JSON slug responses, preventing 404s and data loss on metadata saves.
2026-06-16 DECISION Added a Thumbnail column to Pieces and manual "Generate Thumbnail" buttons on rows in both pieces and platform collections list views.
2026-06-16 DECISION Dark-mode contrast regressions caused by bright accent surfaces inheriting scheme-flipped text were corrected site-wide across all style presets, and row-level actions for pieces/platform collections were unified to one lightweight button pattern.
2026-06-16 DECISION Public member profiles now live at `/user/{username}` with `/user/settings` for profile and style preferences. Comments require sign-in, and owners can now edit or soft-delete their own comments on posts, pieces, exhibits, and exhibit collections.
2026-06-16 DECISION Owner-edit comment forms stay hidden until explicitly opened, and blog feed cards now expand inline by replacing the preview slot while keeping an always-visible Expand/Collapse toggle.

2026-06-17 DECISION Search/filter/sort + infinite scroll are now fully implemented on all 6 public archive pages (/pieces, /collections, /portfolio/pieces, /portfolio/platform-collections, /portfolio/exhibits, /portfolio/exhibit-collections) and the blog. Shared UI pattern: content-filter-bar + filter-bar-primary (search + button inline) + filter-bar-secondary <details> with chip fieldsets (SORT everywhere; TYPE/engine chips only on pieces pages; Category chips on blog). All search labels are sr-only — no visible text prefix before any input. data-listing-status is visually hidden site-wide. Infinite scroll uses the +1 trick (fetch PAGE_SIZE+1, derive hasMore, no COUNT query) with filter-aware $fetchUrl so batch requests preserve active filters. /portfolio gallery page (See More buttons per section) is intentionally untouched.
2026-06-17 DECISION Public canonical/syndication origin now resolves from `site_settings.canonical_public_url` first, then `PUBLIC_SITE_URL`, then the request host. Social-card metadata and emitted public links should follow that resolver so local publishing still points to the public site.
2026-06-17 DECISION Admin navigation is now registry-driven and owner-orderable from `Identity -> Design`; the same order should drive the desktop sidebar, mobile hamburger menu, dashboard cards, and admin account-menu destinations.
2026-06-17 DECISION Admin IA now separates concerns as `Users` (user management only), top-level `AI Settings` (`AI Profiles`, `API Keys`, `AI Vendor`), guided `Platform Connections`, and guided `Feeds` surfaces. Raw JSON/schema editing should stay internal rather than operator-facing.

2026-06-17 DECISION Blog post admin has section-based editing (`post_sections`), inline category creation, "Publish to" platform fieldset with per-platform draft text for Bluesky/LinkedIn, post calendar at `/admin/posts/calendar`, and automatic scheduled-post publishing on every index visit. `posts.content` is always `''`; content lives in `post_sections`.

2026-06-17 NOTE CSS specificity trap: `.form-row { display: grid; }` in `admin.css` beats UA-stylesheet `[hidden]`. Fix: always add `.form-row[hidden] { display: none; }` alongside, and use `element.style.display` in JS (inline style beats everything).

2026-06-17 NOTE `SyndicationPayload::fromPost()` reads `$post['content']` which is always `''` in the section system. Callers must populate `$payload->contentHtml` from `PostSection::allForPost()`. Relative image paths (`/image/123`) must be prefixed with `seo_origin()` before passing to external APIs.

2026-06-17 DECISION Syndication failures are now surfaced: `handleSyndication()` returns a failures array; `postStore()`/`postUpdate()` show a `?syndication_error=` banner on `/admin/posts`. The Syndications tab in Platform Connections shows `error_message` in red.

2026-06-18 DECISION Platform publishing OAuth app credentials for the PHP shell are DB-only, and malformed migrated provider app rows should be normalized to empty placeholders rather than preserved as undecryptable ciphertext.

2026-06-18 CORRECTION Exact session-row parity between the legacy platform DB and the live PHP DB is not a stable deletion-readiness invariant after cutover; session drift should be treated as operational state, not migration failure.

2026-06-19 DECISION Native media uploads/imports are draft-first: `media_files.status` governs readiness, editor/slide pickers only insert `ready` assets, and video posters are linked image assets via `poster_media_file_id`.
Picker and Media Library confirmation must persist `media_files.alt_text` before any image/video asset is considered reusable.

2026-06-19 DECISION The shared Three.js runtime must reconcile OrbitControls after keyboard/click translation so drag/pan preserves the current zoom distance; only wheel/pinch is allowed to change zoom.

2026-06-19 DECISION Managed pages in `draft` are public-404/private-preview: guests should not see them at their public URLs, while signed-in admins may preview them with an explicit draft notice.

2026-06-19 DECISION `site_settings.settings_json` is the compatibility fallback for editable settings like `canonical_public_url` when a dedicated column is missing, so the admin UI never silently drops saved values.

2026-06-19 CORRECTION Any save handler shared by multiple tabs/forms on one DB row must only update fields present in that request's `$_POST` — never rebuild the full row with defaults — or saving one tab silently wipes another's data.
Bit twice in Site Identity: first the logo (Settings vs. Design tabs), then all Settings-tab text (site title, hero/about copy, CTA, copyright). Fixed at the single `updateSettings()` chokepoint in `SiteIdentityAdminController`.

2026-06-19 CORRECTION `public/index.php`'s static/managed-page route and `app/router.php`'s MVC route load different helper sets — always check both load `helpers/auth.php` and `helpers/admin-navigation.php` when adding anything that depends on `current_user()`/admin nav.
Missing requires on the static route made `function_exists()` guards silently behave as logged-out, which is how the nav avatar bug happened — same session showed it working on `/blog` but not `/`.

2026-06-19 DECISION Home and About are the only two protected "system pages" (`Page::PROTECTED_SLUGS`) — undeletable, each auto-seeded/self-healing, each rendering a mandatory top section (Home: Hero+CTA, About: heading+body) from existing `site_settings` fields before normal Pages-CMS sections.

2026-06-19 DECISION URL fields (for pages, categories, collections, exhibits, and posts) were changed from type="url" to type="text" to prevent HTML5 validation failures when picker-selected relative paths (e.g. /image/83) are saved. Constrained Design tab logo preview heights to 60px to prevent layout distortion.

2026-06-19 DECISION Upgraded TipTap link popover and schema to support title, alt, and target attributes vertically in rows, using a DOM-node activeLinkEl fallback to retrieve attributes reliably when the cursor is focused on a link.

2026-06-19 DECISION Added min-width: 0 to the parent .admin-container grid element and max-width: 100% to .admin-table-wrap to resolve CSS Grid horizontal table overflow leaks on desktop.

2026-06-19 DECISION Implemented table-to-card layout conversion below 1024px for Pieces, Platform Collections, Exhibits, and Collections list views, shifting the sidebar collapse breakpoint to 1024px to match, and conditionally disabling drag-reordering under 1024px.

2026-06-19 DECISION Added Sort Order input fields (1-based index) to edit forms and implemented a sequential reordering shift helper (reorder.php) in the database on save/update to shift adjacent items contiguous orders automatically.

2026-06-19 DECISION Removed table min-width: 950px on desktop and added a min-width: 280px constraint for the Actions cell, letting columns size fluids and actions wrap cleanly. Overrode table and actions cell min-widths to 0 inside the mobile media query, preventing card cutoff and wrapping buttons inline on mobile/tablet.

2026-06-19 DECISION Integrated editor blur, window scroll, and immediate link-unset listener resets in tiptap-editor.js to ensure the floating link trigger button hides immediately and doesn't remain fixed in mid-air.

2026-06-19 DECISION Inlined the self-contained Three.js runtime loader (importing three and OrbitControls from absolute CDN URLs) inside piece-render.php to bypass Safari's security blocks on relative dynamic module imports inside sandboxed srcdoc iframes.

2026-06-19 DECISION OAuth login for admin and member now shares one callback URL per provider (/auth/google/callback, /auth/github/callback), dispatched internally by pending session state rather than URL path. Splitting per-flow callbacks would need 4 registered OAuth apps instead of the 2 actually held.

2026-06-19 CORRECTION Site-wide sign-in (one login/logout covers both admin and member sessions) is a standing constraint that silently regressed before this session. Watch for this any time auth.php, AuthController, or UserAuthController are touched in isolation from each other.

2026-06-19 PREFERENCE Describe DESIGN.md taste/identity language by structure, materials, and feel (borders, shadows, scale, tone), not specific color names. Corrected a color-specific Derived Identity draft to this effect.

2026-06-19 CORRECTION The wheel-zoom-preserving invariant from the entry above (only wheel/pinch changes zoom, drag/pan preserves it) regressed silently: an unrelated refactor (media_files schema work) deleted the `onThreeWheel` handler that made it true, with no replacement, and nothing caught it until the user re-encountered the exact bug. Restored verbatim in `public/assets/js/immersive-gallery.js`; this is the kind of regression Rule 7 (below) now exists to gate.

2026-06-19 DECISION Added AGENTS.md Rule 7: never knowingly harm existing, working functionality. Any action in any mode — including mid-execution of an already-approved plan — that's foreseeably risky to something that currently works needs its own explicit approval; a prior plan approval doesn't cover risk discovered while executing it. Mirrored as a short pointer in CLAUDE.md.

2026-06-20 CORRECTION Every save on the piece edit form (manual or AI-Refine-originated) used to update the current `art_piece_versions` row in place instead of creating a new one, so there was no real version history to revert through. Fixed: any code-changing save now creates a new version (metadata-only saves still don't). `art_piece_versions` gained nullable `ai_profile_id`/`ai_persona_id` (no FK — shows "(Blank)" if unset or the referenced row is later deleted), editable on the piece edit form and per-version, and shown on `/pieces/{id}` and `/immersive/pieces/{id}` in one combined block: Engine, AI Profile, AI Persona, Prompt — no separate "AI Attribution" heading.

2026-06-20 DECISION AI Refine (`POST /admin/pieces/refine-ai`) genuinely refines, not regenerates: confirmed the model receives the persona's `system_prompt` (when selected), the piece's original creative prompt, the refinement instruction, and the current HTML/CSS/JS verbatim. `art_piece_refine_user_prompt()` needed an explicit `$originalPrompt` param added to include the original intent — it was previously missing, which likely contributed to refinements drifting from the source piece. The Versions admin list now has "Preview" (any version via `?version={vid}`, no new rendering code) and "Revert" (the existing safe set-current action, relabeled and confirmed) next to Edit/Delete.

2026-06-20 CORRECTION AI Refine was still inferring too much and changing unrequested details (decorations, a figure's clothing, etc.) even with current-code-as-context. A prompt instruction alone can't guarantee an LLM's behavior, so two layers were added: (1) `art_piece_refine_system_prompt()`/`art_piece_refine_user_prompt()` now state an explicit minimal-edit rule — change ONLY what's named, copy everything else verbatim, interpret ambiguous scope narrowly — repeated once in the system prompt and again right next to the instruction in the user prompt; (2) the AI Refine tab now renders a line-by-line diff (vanilla-JS LCS, no new dependency) of HTML/CSS/JS before Accept/Reject, so any unrequested change is visible and catchable by the admin even if the AI doesn't fully comply with the instruction.

2026-06-20 DECISION The minimal-edit prompt instruction (entry above) still wasn't enough — a real refine request rewrote nearly an entire piece despite the diff view catching it. Replaced full-file regeneration in `refineAi()` with a structural fix: the AI must respond with a `PLAN:` (naming specific existing elements it intends to touch, the same discipline Claude Code uses in plan mode) followed by `PATCH <html|css|js>:` blocks expressed as exact `<<<<<<< SEARCH/=======/>>>>>>> REPLACE` find-and-replace edits against the *current* code — never a rewritten file. `art_piece_apply_refine_patches()` applies each patch as a literal substring replacement; anything not captured in a matched SEARCH region is carried forward byte-for-byte, because it's never regenerated at all — this is a guarantee, not a request. A patch that doesn't match exactly once (not found, or ambiguous) fails and retries via the existing loop with a dedicated `art_piece_refine_repair_prompt()`. The AI's PLAN text is now shown above the diff in the AI Refine tab. Initial generation (`generate()`) is untouched — full-file output is correct there since there's no existing code to preserve. 14 new regression tests added, including a reconstructed fixture of the exact failure that prompted this.

2026-06-20 CORRECTION The very first real call to the new plan-then-patch protocol (entry above) silently "succeeded" with zero changes applied — a response with no parseable PATCH blocks at all wasn't distinguished from "this one file legitimately needs no edit," so the original code was returned unchanged and reported as a successful refinement. Fixed: `refineAi()` now treats "all three patch arrays empty" as a failed attempt, retrying via the existing loop with an explicit "must include at least one PATCH block" message, instead of silently no-op'ing. Also made the SEARCH/REPLACE regex case-insensitive and started logging the raw AI response (truncated) in the `ai_refine_piece` audit log on both success and error, since the lack of that logging is exactly what made this failure hard to diagnose precisely the first time.

2026-06-20 CORRECTION Every version of a piece showed the same "Prompt" (the original creative prompt) regardless of what each AI Refine actually changed, because the only save path (`update()`, the main form's "Save Changes") always read the Metadata tab's original-prompt field, never the AI Refine tab's refinement instruction. Fixed with a new dedicated endpoint, `POST /admin/pieces/[id]/refine-save`, called automatically when "Accept Changes" is clicked — it creates the new version with `prompt` set to the actual refinement instruction, and `update()` is otherwise untouched (manual tab edits still fall back to the Metadata prompt, which is correct for them — there's no "instruction" for a hand edit). This also means Accept now saves immediately with an inline "Saved as Version N" status — no more separate "Save Changes" step required after accepting. Per-version `prompt` stays editable by hand on the version edit page either way.

2026-06-20 NOTE Piece 73's `art_piece_versions.id=142` (version 2) had its `prompt` corrected via a one-time direct UPDATE to the user's exact verbatim refinement text, per explicit instruction — not derived or guessed. Version 1 (`id=136`) was left untouched.

2026-06-20 CORRECTION AI Refine failed outright on a Three.js piece (5/5 attempts rejected, "SEARCH text did not match exactly") — not Three.js-specific, confirmed general: `art_piece_refine_repair_prompt()` never re-sent the current HTML/CSS/JS on retries 2-5, only the failure message, so the AI was retrying blind from memory of its own mistake — now fixed by passing `$html`/`$css`/`$js` through on every retry. Also added `art_piece_find_patch_match()`: exact match first, falling back to a whitespace-tolerant match (tokenize into word-runs + individual punctuation, discard whitespace, rejoin with `\s*`) when exact fails — tolerates the incidental reformatting LLMs introduce when transcribing code (extra/missing space around `{`/`:`/`,`) without becoming content-fuzzy; every real token still must match. Three.js triggers this more because its lines (object-literal constructor calls) are longer and denser, not because the underlying weakness is engine-specific.

2026-06-20 DECISION Raised output token limit for OpenCode Go and Zen models to 16384 to support verbose reasoning.
Enables reasoning-heavy models (e.g. minimax-m3) to fully output PLAN and SEARCH/REPLACE patches without early cutoff.

2026-06-20 DECISION `AiProviderClient::generate()` (shared by piece generation and AI Refine) was unconditionally appending a "skip planning notes, output only fenced code blocks" instruction — written for the old full-file generation format — that directly contradicted Refine's PLAN+PATCH protocol, which requires a PLAN section. Added `suppressPlanningPreamble` (default `true`, preserving generation's exact prior behavior) and `maxTokensOverride` params; `refineAi()` passes `false`/`24576`, accounting for a patch's higher output cost (verbatim SEARCH anchors on top of REPLACE content). Also added `finishReason` extraction and `finishReasonMeansTruncated()`, so a token-limit-truncated response now surfaces a distinct, actionable error instead of the generic "no valid PATCH blocks" message.

2026-06-20 CORRECTION The actual cause of every Three.js AI Refine failure this session (and the latent cause for P5.js/C2.js) was a label mismatch, not a token limit or contradictory instruction: models reliably write `PATCH javascript:` instead of the required `PATCH js:`, and `art_piece_extract_refine_patches()` only ever recognized the literal `js` token — silently discarding otherwise perfectly valid, correctly-targeted patches. SVG pieces never hit this because their refinements are HTML-led; Three.js/P5.js/C2.js refinements are almost always JS-only and hit it on nearly every attempt. The system prompt's one worked example was HTML-only, giving the model nothing to anchor the exact abbreviation to. Fixed by accepting `javascript` as a synonym for `js` in the parser (normalized on parse) and adding a second worked example (`PATCH js:`) with an explicit "always `js`, never `javascript`" line. Verified live against real pieces across Three.js, P5.js, and C2.js engines, not just inferred from the fix's design.

2026-06-20 NOTE When an AI Refine failure can't be explained from the audit log alone (`audit_log_redact_value()` hard-truncates any string metadata value, including `raw_response`, to 500 chars regardless of upstream truncation), reproduce it directly: a standalone CLI script that loads `public/.env`, requires `database.php`/`schema.php`/`encryption.php`/`art-piece-generation.php`/`AiProviderClient.php`/`UserAiSettings.php`, and calls the exact same `generate()`/`extract`/`apply` functions against the real failing piece, profile, and instruction. This bypasses the browser and the log's truncation entirely and is what actually found the `javascript`-vs-`js` bug, after three rounds of guessing from log snippets alone hadn't.

2026-06-20 CORRECTION Thumbnail capture (P5.js/C2.js/Three.js) never actually waited for a real drawn frame, only a proxy for one: P5/C2 had no ready signal at all (capture fell through to a blind timeout), and Three.js posted its signal unconditionally right after setup, before either render path had painted anything — reliable on desktop only because existing fixed buffers happened to be generous enough, the same latent risk on a slower mobile connection that the user recalled hitting before. `piece-runtime.js` gained a shared `signalCanvasReady()` fired only after an actual frame: P5 polls `instance.frameCount >= 1`; C2/generic wraps the `startFrame` handed to the sketch; Three.js fires from both its possible render paths plus an end-of-function fallback. Manual capture (`form.php`/`index.php`) now requires this marker (not just canvas existence) and moved its capture iframe from `left:-9999px` (genuinely off-screen, where some browsers throttle `requestAnimationFrame`) to `opacity:0` while staying in-viewport. `index.php` has its own accepted inline duplicate of the boot logic (per the existing test suite's carve-out) — mirrored identically. Could not verify live; no browser automation tool is available here.

2026-06-20 CORRECTION `CreatrImmersiveImage` (embed.js) embeds the full `/immersive/images/{ref}` page directly, same as `CreatrExhibitWall` — but only `CreatrExhibitWall` had `installFullscreenWrapperProtocol()` wired into its constructor, the listener that promotes an embedded immersive page's Expand button to a real viewport-filling overlay on iPhone Safari (which has no Fullscreen API at all). `CreatrArtPiece` genuinely doesn't need it (separate minimal embed route, no fullscreen affordance). Fixed by mirroring `CreatrExhibitWall`'s constructor call and matching `:host(.creatr-fullscreen)` CSS into `CreatrImmersiveImage`. Could not verify against a real iPhone Safari session.

2026-06-20 CORRECTION "ERR_CONNECTION_CLOSED" on production saving a piece was not resource exhaustion (confirmed via the user's own Hostinger panel graphs: workers/processes nowhere near capacity), not a payload-size limit (checked actual code+thumbnail sizes directly from the DB across all 4 engines — all far below `post_max_size`), and not engine- or width/browser-specific (`generateSave()` branches on nothing engine-related; grepped zero `innerWidth`/`matchMedia`/`userAgent` conditionals in the relevant files). Most likely cause: a dead/stale keep-alive connection reused for the separate Save POST after a gap (~15-30s) spent reviewing the Generate preview — browser-agnostic in mechanism (any browser reusing a connection the server already closed fails identically), with iPhone Safari plausibly just the likeliest *place* to hit it since iOS backgrounds tabs more aggressively than desktop. Fixed by converting Save (`generate-preview.php` + `PiecesAdminController::generateSave()`, confirmed exactly one caller) from a traditional POST-redirect to `fetch()` returning JSON, with a one-time automatic retry on a network-level failure specifically (not a resolved server error, which still surfaces normally). Verified both response shapes end-to-end via a CLI harness against the real DB (created and immediately deleted a test piece for the success case). Also added a live elapsed-time tick to Generate, Refine, and Save, which previously only showed a static "Generating…"/"Refining…" label with no sense of how long the wait had been.

2026-06-20 CORRECTION AI piece generation and preview save are now fetch/session-based in the PHP Hostinger app: generation returns JSON, progress is session-polled, preview is a one-time session flash route, and Save has no native form POST path to the JSON endpoint.

2026-06-20 CONSTRAINT augmenthumankind.com production runs on Hostinger/PHP; the platform/Replit app is reference-only and must not be treated as the runtime target.

2026-06-20 DECISION AI Refinement and generation have been strengthened: (1) HTML code is isolated and excluded from refinement for non-SVG pieces (p5, c2, three), guaranteeing canvas container preservation; (2) SVG pieces use adaptive checks to prevent removing the <svg> element; (3) CSS checks prevent hiding the canvas/SVG; (4) line endings (\r\n vs \n) are normalized in all controller saves, preventing duplicate version generation on edit forms.

2026-06-20 DECISION AI Refine banner was made responsive (placing text above action buttons, wrapping buttons, styling as inline-block) and the active tab now switches to "AI Refine" on request to keep status loading text visible.

2026-06-20 CORRECTION HTML isolation for p5/c2/three (DECISIONS.md, "AI Refinement and generation have been strengthened") was confirmed already working and unchanged across "Request Stronger Change" iterations — but a Three.js piece (ID 83) still hit "No canvas or svg element found after waiting" on a second iteration. Actual cause: `art_piece_preflight_code()`'s `window.sketch` check in `art-piece-generation.php` only matched the substring `window.sketch` appearing anywhere in the code, not an actual assignment — a patch that left it merely referenced (e.g. `typeof window.sketch === 'function'`) passed validation, then `piece-runtime.js` silently no-ops (no canvas, no error) when `window.sketch` isn't really a function, which is exactly what the capture script's 20-second poll times out on. Fixed by tightening the regex to require a real `=` assignment (excluding `==`/`===`). Runs inside the existing 5-attempt self-healing retry loop shared by generation and refine, so a bad patch now gets caught and auto-repaired in-request instead of failing silently at preview time. 56/56 tests pass; could not verify a live round-trip (no browser automation available this session).

2026-06-21 CORRECTION The same piece (ID 83) hit two more failures right after the window.sketch fix above — but reading its actual saved code directly from the production DB (explicit user approval given for this read) showed only one version ever saved, with fully valid `window.sketch = (runtime) => {...}` code. Neither failure was AI-output-related: (1) "AI Refinement Error: Load failed" is `fetch()`'s own network-layer error message (a dropped/stale connection) — the same class as the "ERR_CONNECTION_CLOSED" Save bug fixed earlier, whose fetch-with-one-retry fix was never applied to the Refine endpoint; now mirrored into `form.php`'s `requestAiRefine()`. (2) The subsequent "No canvas or svg" on plain Save was capturing the piece's own valid, already-saved code — on mobile (2-bar signal in the screenshot), exactly the latent slow-connection risk flagged but unverified in the 2026-06-20 readiness-signal fix: Three.js loads `three.module.js`+`OrbitControls.js` from a CDN before any canvas exists, and that import can outlast the ~27s capture budget on a poor connection. Raised the three-engine poll ceiling in `admin-piece-capture.js` from 40 to 70 attempts (only costs time on the genuinely-stuck path, loop still breaks the instant a ready canvas appears). 56/56 tests pass; could not verify a live retry/slow-network round-trip (no browser automation available).

2026-06-21 CORRECTION The raised poll ceiling above did NOT fix piece 83 — it still failed "No canvas or svg" on WiFi, and the main edit-form's Save hit `ERR_CONNECTION_CLOSED`. Reading version 2's actual saved code (id=155, via the same approved DB read) showed it was completely valid — the real cause was the refined scene looping `new THREE.Mesh(...)` 900+280+1100 times for "denser hair/beard," ~2,388 individual mesh objects with no instancing, plausibly exhausting WebGL resources / losing the context during construction — something `piece-runtime.js` had zero listener for, so it fails with no error and no ready signal, which is why a longer timeout never helped (the renderer never comes up at all, and bandwidth was never the bottleneck). Added `art_piece_count_three_object_calls()` in `art-piece-generation.php`: my first attempt at this check used a flat source-text count of `new THREE.Mesh(` call sites, which only found "28" for this exact code — completely missing that one call site inside a 900-iteration loop creates 900 runtime objects. Caught this by testing the check against the real saved code before trusting it, not by inspection. Replaced it with a loop-aware count that detects bounded `for (...; i < N; ...)` loops and multiplies mesh calls in the body by N; correctly returns 2,388 for the real code and now rejects anything over 150 (with `InstancedMesh`/merged-`BufferGeometry` guidance), inside the same 5-attempt self-healing retry loop as every other check in that function. Also: converted `form.php`'s main Save submit (the dirty/code-changed branch only) from native `form.submit()` to the same fetch()+one-retry pattern already used by `fetchRefine()` and `generate-preview.php`'s `submitSave()` — `PiecesAdminController::update()` now branches to JSON on `X-Requested-With: XMLHttpRequest`, leaving the non-JS redirect/inline-error behavior untouched. Added a `webglcontextlost` listener in `piece-runtime.js`'s `bootThree()` so any future context loss (e.g. from heavy textures the mesh-count check doesn't catch) surfaces as a real error instead of a silent timeout. 59/59 tests pass; could not verify a live Save round-trip or live mobile capture (no browser automation available).

2026-06-21 CORRECTION Immediately after the mesh-count fix above, a fresh AI Refine attempt on piece 83 failed with a brand-new, more cryptic error: "AI Refinement Error: The string did not match the expected pattern." — Safari/WebKit's generic SyntaxError (DOM Exception 12), thrown by `JSON.parse`/`res.json()` for ANY malformed input across many unrelated APIs; one of its most common causes is literally "JSON parsing of non-JSON responses" (confirmed via web search, not guessed). Checked `audit_log_events` directly (same approved DB access): zero new rows in 46 minutes of DB time despite the user actively waiting "3:00 elapsed," for ANY scope — meaning the request never reached either of `refineAi()`'s own try/catch blocks, which both log before responding. Since the error text changed from the earlier "Load failed" (a `fetch()`-level network rejection, already retried) to a JSON-parse failure, the request DID get a response body back — just not valid JSON. Conclusion: this app's hosting infrastructure (Hostinger shared hosting, per the existing CONSTRAINTS.md note) has its own proxy/PHP-FPM request timeout, separate from and shorter than this script's own `set_time_limit(660)`, and real per-attempt durations logged in `audit_log_events` for this exact piece/model (opencode-go/minimax-m3) ranged from ~20s to ~119s — so 5 sequential attempts can plausibly exceed that external ceiling, which then kills the connection and returns a non-JSON error page that `res.json()` can't parse. Cannot inspect or change Hostinger's actual proxy timeout value from this repo (confirmed no nginx/Apache config for it exists in-repo; it's managed server-side). Mitigated on both ends since the real number is unknown: (1) `refineAi()`'s retry loop now stops *starting new* attempts (never aborts an in-flight one) once total elapsed exceeds ~240s, returning a clean, valid JSON error well before the existing 660s PHP limit instead of letting up to 5 slow attempts run unbounded; (2) `form.php`'s `fetchRefine()` consumer now reads the response as text and tries `JSON.parse()` itself, surfacing an honest "request took too long and was cut off" message on failure instead of the cryptic native browser error, regardless of whether the 240s backstop above fully closes the gap to whatever Hostinger's real limit is. 59/59 tests pass; could not verify either fix against a live slow/long-running request in this session (no browser automation, and deliberately did not re-trigger a real AI Refine call against production to avoid spending more of the user's tokens/time chasing the same failure).

2026-06-21 DECISION Replaced `refineAi()`'s automatic internal 5-attempt loop (root cause of the previous several entries' chained-timeout risk) with a client-driven design: each call now does exactly one AI attempt; `form.php` shows a styled `dialog#refine-attempt-failed-dialog` (reusing the existing `.inline-create-dialog` pattern, not a native `alert()`) on failure, asking the user to spend another attempt (up to 5 total) or give up — so tokens are only spent on retries the user actually wants, and the chained-attempts infra-timeout risk from the prior several entries mostly disappears since one attempt at this vendor/model's pace comfortably fits the external timeout. Per explicit user requirement, every attempt's extracted code (success or failure) is now persisted immediately as its own `art_piece_versions` row flagged `is_draft_attempt = 1` (new migration, `attempt_sequence_token` groups one retry sequence) — never selectable as current, but viewable/editable/forkable via a new "Fork as New Piece" action, so no tokens spent are ever silently thrown away. On Accept, `refineSave()` promotes the successful draft in place (re-writing its code in case the admin hand-edited the proposal first) and deletes the sequence's failed siblings; abandoning a sequence without success leaves all its drafts intact. Audited every other read of `art_piece_versions` for draft leakage and found one real risk: `PlatformArtPiece::attachCurrentVersion()` (the shared loader behind nearly every piece-fetching method, admin and public) had no draft filter and a `$versions[0]` fallback that could have silently treated a draft as "current" for a piece where every attempt had failed — fixed there once, covering every caller. Scoped to AI Refine only, by explicit instruction; the schema is named generically so piece generation can adopt the identical pattern later. Verified live against the production DB (explicit approval obtained, same as the schema migration itself): created and fully cleaned up a temporary test piece exercising the draft/promote/delete-siblings logic end to end, all assertions passed — and that live check is what surfaced a pre-existing, unrelated `?:`-vs-`??` undefined-array-key warning in `PlatformArtPieceVersion::create()`/`update()`, fixed in the same pass since it was directly adjacent to lines already being touched. Could not exercise the new dialog UI in a real browser this session (no automation available) — the next real AI Refine attempt is the actual end-to-end verification.

2026-06-21 CORRECTION The new retry feature above worked exactly as designed on its first real test (piece 83, 5-attempt sequence) — but all 5 attempts failed, giving real evidence to act on instead of theory. Pulled the 3 draft versions with extractable code directly from the DB: loop bounds were reshuffled each attempt (600+120+40+500 → 450+160+600+40 → 600+120+40+700) but `InstancedMesh`/`mergeGeometries` appeared in NONE of them — the model never once tried instancing, because neither `art_piece_generation_system_prompt()`'s three.js rules nor `art_piece_refine_repair_prompt()` ever mentioned it as an option; the model only learned the 150-object cap existed from a single unframed line in the rejection message, which it then ignored on retry rather than restructuring its approach. Fixed by adding explicit InstancedMesh/`setMatrixAt` guidance + a worked code snippet to the shared three.js system prompt (refine inherits this automatically since `art_piece_refine_system_prompt()` embeds the same base rules verbatim — no separate change to generation's prompt or flow needed) and by reframing the repair prompt's failure message as "CRITICAL... you MUST directly address this specific problem... state the new approach, not just adjusted numbers." User raised an alternative theory (the 2 of 5 attempts that failed on a 120s provider timeout happened because retries fired too soon) — checked the actual screenshot timestamps (2-3 minutes apart, not rapid-fire) and the existing `ai_refine_piece` rate limiter (6-per-15-minutes cap, no minimum-spacing rule at all) — neither supports that as the cause of these two specific timeouts, said so plainly rather than building a fix on an unconfirmed premise. Added the requested 30s cooldown timer on "Try Again" anyway as a reasonable debounce regardless of cause, but flagged `REFINE_RETRY_COOLDOWN_SECONDS = 30` in `form.php` explicitly as preliminary/unverified per the user's own instruction — if the same timeout pattern recurs after this ships, 30s didn't fix it and the real cause is still open. Also extended "Fork as New Piece" (`versions.php`) to every version, not just failed drafts — confirmed via direct code read that `versionFork()`/its route had zero draft-specific logic already, so only the view's button-visibility condition changed; the Revert gate (drafts can never become current) is untouched. 59/59 tests pass; could not verify the model's actual compliance or whether 30s is adequate without spending the user's real tokens on a live call — explicitly not claimed as fixed.

2026-06-21 CORRECTION The 30s cooldown above was never the fix: the user directly tested it by waiting 60+ seconds between two attempts and still hit the identical 120002ms timeout — conclusively disproving "retried too soon," don't re-litigate this. The mesh-count threshold (150, set two entries ago) was also wrong, and this time checked against real data instead of picked: audited all 22 currently-live Three.js pieces in production, found real counts of 2-578, with 3 *already* exceeding 150 (up to 578) and all rendering fine (successful thumbnails) — piece 83's own original working baseline is itself 578 objects, meaning the validator was rejecting a count the piece had already proven safe before validation even existed. Raised the threshold to 1000 (real headroom above 578, still rejects the earlier 2,388-object disaster). Separately, audit_log_events showed attempts 2 and 4 (both asked to actually restructure to InstancedMesh) timed out at exactly 120002ms — Guzzle's ceiling — while attempts 1/3/5 (simpler regenerate tasks) finished in 45-87s: the harder task is genuinely slower for the model, not a timing/spacing issue. Gave `AiProviderClient` an optional `$timeoutOverride` param (default 120.0 unchanged for every other caller — generation, ai_process_text, ai_describe_image); only `refineAi()` passes 180.0, paired with raising its own `set_time_limit` from 150 to 220. Also read the actual 578-object code: the model DID use InstancedMesh for "top hair" (the prior fix partially worked) but not beard/mustache, because those strands have per-instance *varying geometry size* and *per-strand phase-animation* — two patterns the worked example never showed combined with instancing. Extended the example with non-uniform per-instance scale (simulating size variation without a new geometry) and per-frame `setMatrixAt`+`instanceMatrix.needsUpdate` updates inside `startFrame`. 59/59 + 11/11 tests pass; directly re-verified piece 83's real baseline (578) now passes preflight. Could not verify the model's actual compliance with the extended example, or whether 180s actually resolves the timeout, without a live call spending real tokens.


2026-06-21 DECISION The 150→1000 mesh-count recalibration above was tested live and falsified within hours: a 728-object piece under the 1000 cap still failed to render ("No canvas or svg" — the original symptom), while the user separately confirmed a 1400+ object piece rendered fine. Object count never predicted renderability at any number tried (150, then 1000) — what matters is whether objects are instanced, not how many there are. Per explicit, repeated instruction (the user rejected an early draft of this plan specifically because old superseded threshold language was still visible in the plan file, confirming exactly how strongly this needed to be unambiguous): removed the mesh-count rejection from `art_piece_preflight_code()` entirely — no replacement number, no warn-only softer version, no future reintroduction. `art_piece_count_three_object_calls()` itself stays (harmless, tested, still correct) but is never called as a gate again; optionally threaded into `refineAi()`'s audit log as a silent `mesh_object_count` diagnostic field, never surfaced to or blocking the user. The InstancedMesh system-prompt guidance stays as voluntary advice. 58/58 + 11/11 tests pass (replaced the threshold-specific tests with one confirming a 2,500-object piece now passes cleanly). This is the final word on this mechanism — do not propose a third threshold number in a future session.

2026-06-21 CORRECTION Right after the mesh-count removal above, the user's next real test still hit "No canvas or svg element found" on the rendered comparison, on a *successful* attempt that correctly used InstancedMesh — and a separate attempt failed with a bare "Internal server error." Traced "Internal server error" into `AiProviderClient::generate()`'s non-2xx handling: it's the AI provider's (opencode.ai/zen) own error message extracted from their response body, not ours (grepped the whole codebase, zero matches) — already handled correctly by the existing retry dialog (attempt 2 succeeded). For the "No canvas" recurrence: checked piece 83's actual current live version directly — still the same 578-object, non-instanced baseline already confirmed rendering fine standalone via the single-capture "Generate Thumbnail" path. Since the comparison's "before" side captures this exact unchanged, known-good code, the cause can't be the generated code's content. Found it: `form.php`'s `handleRefineAttemptSuccess()` ran the before/after captures via `Promise.all` — two full concurrent Three.js captures, each needing its own CDN fetch of `three.module.js` and its own WebGL context, doubling simultaneous load exactly where the single-capture path (which has been reliable all session) never does. Changed to sequential captures (before fully, then after) — also lets the second capture reuse the browser's now-warm CDN cache from the first. Could not verify this actually fixes it on a real connection without the user's next live test; if "No canvas" recurs on an otherwise-confirmed-working baseline after this ships, the concurrency theory is also wrong and needs a different signal (e.g. distinguishing which side of the comparison actually failed, which the current error message doesn't do).

2026-06-21 CORRECTION The sequential-capture fix above did not resolve "No canvas or svg" — but the user's next report included a side-by-side: the exact same generated code rendered fine in the edit form's own live preview iframe, on the same page load where the capture comparison still failed. Compared the two iframes directly: `updateLivePreview()` builds a normal, default-opacity iframe; `admin-piece-capture.js`'s `capture()` uses `opacity:0;position:fixed;z-index:-1` (changed from literal off-screen positioning several sessions ago specifically to dodge rAF throttling). `opacity:0` was the only difference between an iframe that worked and one that didn't for byte-identical code — WebKit is known to skip real WebGL backing-store allocation for opacity:0 content as an optimization. Removed `opacity:0`, kept `z-index:-1` (sufficient alone since the page body has its own opaque background, confirmed in styles.css) — this is the one shared `capture()` function used by both AI Refine's comparison and the regular thumbnail/Save flows, so it applies everywhere uniformly. This is the most directly-evidenced fix in this entire investigation (same code, two pathways, one variable changed) but still unverified live — if "No canvas" recurs after this, the next step is instrumenting the capture script itself to report which specific condition it's still waiting on, not inferring from outside any further.

2026-06-21 CORRECTION Fixed the premature clearing of the AI Refine loader/timer and the WebGL/rAF capture iframe throttling. (1) In `form.php`'s `performRefineAttempt()`, deferred the `stopAndReenable()` call (which stops the timer, resets the status, and enables buttons) from running immediately when the API call finishes. Instead, update the status label to `'Generating preview snapshots'` so the elapsed timer keeps ticking and buttons remain disabled during the long visual capture. The `stopAndReenable` callback is now executed only after `handleRefineAttemptSuccess()` completes the capture chain or encounters an error. (2) In `admin-piece-capture.js`, replaced `z-index: -1` (occluded behind the opaque body) with `z-index: 999999` and a tiny non-zero opacity (`opacity: 0.002`) and `pointer-events: none` (overlay-opacity approach). WebKit and other modern engines aggressively throttle requestAnimationFrame and suspend code/network execution inside hidden or completely occluded iframes. Putting the capture iframe on top with a non-zero opacity forces the browser compositing culling to treat it as active, preventing rAF throttling and module loading suspension while keeping it completely invisible and click-safe to the user. All 69 unit tests pass.

2026-06-21 DECISION Decoupled the visual thumbnail/comparison capture from browser visibility/compositing culling by shimming window.requestAnimationFrame with a setTimeout polyfill inside the capture iframe when source.isCapture is true.

2026-06-21 DECISION Non-SVG engines (p5, c2, three) have their HTML containers locked backend and frontend to standard boilerplate, hiding the HTML editing tab in admin views.
Enforces `<div id="canvas-container"></div>` for p5, `<canvas id="piece-canvas"></canvas>` for c2, and `<div id="container"></div>` for three.

2026-06-21 CORRECTION WebKit occlusion culling of the capture iframe on iOS Safari was resolved by wrapping the 960x540 iframe inside a 1px by 1px overflow-hidden parent div on top.
Bypasses compositing culling while avoiding viewport dimension collapsing caused by CSS scaling.

2026-06-21 CORRECTION Overhauled `window.CreatrPieceCapture.capture` to extract canvas data/SVG directly from the active live preview iframe before falling back to background iframe creation, preventing Safari/WebKit resource culling timeouts on form saves.

2026-06-21 CORRECTION Implemented `lastDialogClosedAt` timing checks in `form.php` to discard synthesized click events within 500ms of closing the failure dialog, preventing ghost clicks from launching concurrent AI refinement attempts.

2026-06-21 CORRECTION Reinforced disabled button styling in `admin.css` with `pointer-events: none !important` to ensure disabled buttons are unclickable and visually inert on mobile browsers.

2026-06-21 DECISION bootThree() must create the canvas before its CDN imports, not after.
Otherwise capture can't tell a slow CDN apart from a piece with no canvas — see [piece-runtime.js](public/assets/js/piece-runtime.js).

2026-06-21 DECISION performCapture() must run on every path that saves new piece code, not just one.
Save Changes and AI Refine Accept both create new code; AI Refine Accept previously skipped thumbnail capture entirely.

2026-06-21 CORRECTION "No canvas or svg element found" from admin-piece-capture.js is a fallback timeout message, not a real claim about the piece.
Check piece-runtime.js's CDN import path and any sketch-status runtimeError before trusting it at face value.
