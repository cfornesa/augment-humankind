# .env.example — copy to .env if future local configuration is needed.
# Never commit .env to version control.
# See DECISIONS.md → Environment Variables Required for
# the full list of variables this project needs.

# Augment Humankind v1 does not require environment variables.
